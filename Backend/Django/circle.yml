machine:
  services:
    - docker

dependencies:
  override:
    - bash setup-heroku.sh
    - docker pull mysql
    - docker build -t docker-api-devf .
    - docker run --name django-mysql -e MYSQL_ROOT_PASSWORD=Derzitteraal666 -e MYSQL_DATABASE=danmificadosDb -d mysql 

test:
  override:
    - docker run -it --link django-mysql:mysql -e DJANGO_ENV=development docker-api-devf

deployment:
  qa:
    branch: master
    commands:
      - sh deploy.sh